import {makeStyles} from '@material-ui/core/styles';

const useStyles = makeStyles(
  (theme) => ({
    root: {
      
    },
    testHeading: {
      fontWeight: '1000',
      fontSize: '36px'
    }
  }),
  {index: 1},
);

export default useStyles;