import {makeStyles} from '@material-ui/core/styles';

const useStyles = makeStyles(
  (theme) => ({
    root: {
      
    },
    imgContainer: {
      
    },
    loungeImg: {
      margin: 'auto'
    }
  }),
  {index: 1},
);

export default useStyles;