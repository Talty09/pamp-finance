import {makeStyles} from '@material-ui/core/styles';

const useStyles = makeStyles(
  (theme) => ({
    paper: {
      width: '100%',
      padding: 20,
    },
  }),
  {index: 1},
);

export default useStyles;
